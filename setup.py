from setuptools import setup, find_packages

setup(name='car_wash',
      version='0.0.1',
      author='Eduardo Ferro',
      description='Dummy sample for SOLID presentarion',
      platforms='Linux',
      packages=find_packages(exclude=['ez_setup', 'examples', 'tests']),
 )
